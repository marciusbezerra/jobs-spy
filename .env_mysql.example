# Example env for the centralized MySQL compose (copy to `.env_mysql` and fill values)

MYSQL_ROOT_PASSWORD=change_me_root_password

# Per-app database/user (the init script can create these on first startup)
JOBS_SPY_DATABASE=jobs_spy
JOBS_SPY_USER=jobs_spy_user
JOBS_SPY_PASSWORD=change_me_jobs_spy_password

# Add other app-specific vars if you want the init script to create them
# OTHERAPP_DATABASE=other_db
# OTHERAPP_USER=other_user
# OTHERAPP_PASSWORD=other_password
